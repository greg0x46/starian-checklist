{
  "name": "starian-checklist-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Smoke em navegador dos dois serviços: fluxo completo, responsividade e acessibilidade.",
  "scripts": {
    "test": "playwright test",
    "install:browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  },
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  }
}
